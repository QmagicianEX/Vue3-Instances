import {
  create,
  NButton,
  NIcon,
  NFlex,
  NLayout,
  NLayoutSider,
  NMenu,
} from 'naive-ui'

export const naiveUI = create({
  components: [NButton, NIcon, NFlex, NLayout, NLayoutSider, NMenu]
})